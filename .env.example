# Environment Configuration for Image Placeholder Server

# Server Port (default: 3000)
PORT=3000

# Optional: Set NODE_ENV for production optimizations
# NODE_ENV=production

# Optional: Enable debug logging (development only)
# DEBUG=true